#include "./include/minishell.h"











/*
int	set_variable(char **word, ,t_token *token ,t_flag *flag)
{
	int	i;

	i = 0;

	if (*(*word + 1) == '?')
	{
		//ポインタを?の次まで進める
		//子プロセスidが入ったtokenの構造体を間にいれる
		//return (TRUE);
	}
	if (*(*word + 1) == '$' || *(*word + 1) == '\0')
	{
		//ポインタを１つ進める
		//$一文字が入ったtokenの構造体を間にいれる
		//return (TRUE);
	}
	else
	{
		// 次の$が出てくるまたは\0までを変数名として認識する
		//変数名を環境変数から探す
		//あればその文字列をlexcerにかけてtokenに分割した後間に入れる
		//該当する変数が環境変数になければnullの文字列が入った構造体を追加
	}
	//隣のtokenとの間にスペースが入っているかのフラグの管理に気をつける!!!

}
*/




// lexcer


